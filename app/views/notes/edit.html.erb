<h1>Edit note</h1>

<%= form_with url: note_path, method: :patch do |f| %>
    <div>
        <%= f.label :name %>
        <%= f.text_field :name, value: @note.name %>
    </div>
    <div>
        <%= f.label :text %>
        <%= f.text_area :text, value: @note.text %>
    </div>
    <%= f.submit 'Update' %>
<% end %>
<br>
<%= link_to 'Back to Notes', notes_path %>